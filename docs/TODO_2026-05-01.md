# READ VOICE Pro MVP - 작업 목록 (2026-05-01)

**작성일:** 2026-04-30  
**현재 버전:** v2.7.2  
**다음 작업 시작일:** 2026-05-01

---

## 📋 즉시 수정 필요 (우선순위 높음)

### 1. ❌ 접근성 원칙 위반 수정 (CRITICAL)

**문제:** CLAUDE.md의 "장애 언급 절대 금지" 규칙 위반

**수정 대상 2곳:**

#### A. `app/api/chat/route.ts:5`
```typescript
// 현재 (잘못됨)
const systemPrompt = `당신은 시각장애인과 거동불편인을 위한 AI 음성 도우미입니다.`

// 수정 후
const systemPrompt = `당신은 모든 사용자를 위한 AI 음성 도우미입니다.`
```

#### B. `app/page.tsx:962`
```tsx
// 현재 (잘못됨)
<h1 className="...">시각장애인을 위한 AI 음성 도우미</h1>

// 수정 후
<h1 className="...">AI 음성 도우미</h1>
```

**이유:** 사용자에게 장애를 환기시키는 것은 patronizing하고 실례임

---

### 2. ⚠️ Ollama URL 하드코딩 제거

**문제:** Vision 모듈에서 환경변수 미사용

**수정 대상 2곳:**

#### A. `modules/ocr/gemini.ts`
```typescript
// 현재 (하드코딩)
const response = await fetch("http://localhost:11434/api/generate", ...)

// 수정 후
const OLLAMA_URL = process.env.OLLAMA_BASE_URL || "http://localhost:11434";
const response = await fetch(`${OLLAMA_URL}/api/generate`, ...)
```

#### B. `modules/ocr/pdf.ts`
```typescript
// 동일하게 OLLAMA_BASE_URL 환경변수 사용
```

**이유:** 환경 변경 시 유연성 확보, CLAUDE.md 규칙 준수

---

## 🔧 개선 권장 (우선순위 중간)

### 3. LLM 폴백 로직 강화

**현재 상태:** `lib/llm/index.ts`가 단순 팩토리 패턴만 제공 (14줄)

**추가 필요:**
- Ollama 실패 시 자동 Claude API 폴백
- 타임아웃 설정
- 재시도 로직
- 에러 핸들링

**참고:** CLAUDE.md에 "모델 선택, 폴백 로직을 중앙에서 관리" 명시됨

---

### 4. aria-label 검증

**확인 필요:**
- 현재 aria-label 총 9개 (page.tsx: 1, FileUpload.tsx: 6, MicButton.tsx: 1, ResponseDisplay.tsx: 1)
- 모든 대화형 요소 (`<button>`, `<input>` 등)에 적용되었는지 확인
- 누락된 요소 추가

---

## 📊 현재 상태

### ✅ 완료된 항목
- Git: 클린 상태 (커밋 필요 없음)
- 모델 전환: Sonnet 4.6으로 설정 완료
- 핵심 기능: 음성/이미지/OCR/PDF 파이프라인 모두 작동
- 타입 안전성: `any` 타입 미사용

### 📁 파일 구조
```
readvoice-mvp/
├── app/
│   ├── page.tsx (수정 필요: line 962)
│   ├── api/chat/route.ts (수정 필요: line 5)
│   └── components/
├── lib/llm/index.ts (개선 권장)
├── modules/ocr/
│   ├── gemini.ts (수정 필요: URL 하드코딩)
│   └── pdf.ts (수정 필요: URL 하드코딩)
└── docs/
```

---

## 🎯 작업 순서 (추천)

1. **접근성 원칙 위반 수정** (5분)
   - `app/api/chat/route.ts:5` 수정
   - `app/page.tsx:962` 수정

2. **Ollama URL 환경변수화** (10분)
   - `modules/ocr/gemini.ts` 수정
   - `modules/ocr/pdf.ts` 수정

3. **테스트** (10분)
   - 음성 대화 테스트
   - 이미지 분석 테스트
   - PDF 처리 테스트

4. **Git 커밋** (5분)
   ```bash
   git add .
   git commit -m "fix: 접근성 원칙 준수 및 환경변수 개선
   
   - 장애 언급 텍스트 제거 (CLAUDE.md 규칙 준수)
   - Ollama URL 하드코딩 제거
   - OLLAMA_BASE_URL 환경변수 사용
   
   Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
   ```

5. **버전 저장** (2분)
   ```powershell
   .\scripts\version-save.ps1 -Version "v2.7.3" -Message "접근성 원칙 준수 및 환경변수 개선"
   ```

---

## 🚀 내일 시작 시 체크리스트

- [ ] `docs/TODO_2026-05-01.md` 읽기
- [ ] Sonnet 4.6 적용 확인 (`~/.claude/settings.json`)
- [ ] 작업 1: 접근성 원칙 위반 수정
- [ ] 작업 2: Ollama URL 환경변수화
- [ ] 작업 3: 전체 기능 테스트
- [ ] 작업 4: Git 커밋
- [ ] 작업 5: 버전 저장 (v2.7.3)

---

**예상 소요 시간:** 총 30분  
**다음 버전:** v2.7.3  
**목표:** CLAUDE.md 규칙 100% 준수, 코드 품질 향상
